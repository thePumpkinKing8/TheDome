using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Domeling : MonoBehaviour
{
    private PersonPathing _pathing;

    #region Emotions

    #endregion

    private void Awake()
    {
        _pathing = GetComponent<PersonPathing>();
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
